name: 'Vowel Frequency Analyzer'
description: 'Analyzes the frequency of vowels in a given text file'

inputs:
  file:
    description: 'The path to the text file to analyze'
    required: true

runs:
  using: 'python'
  main: 'scripts/frequency.py'  # Executes the frequency.py script located in the scripts folder
